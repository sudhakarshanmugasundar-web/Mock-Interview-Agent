import React from 'react';
import { InterviewHistoryPage } from '../components/InterviewHistoryPage';

/**
 * Route-level page wrapper for the Interview History feature.
 * All logic lives in the InterviewHistoryPage component.
 */
export const HistoryPage: React.FC = () => {
  return <InterviewHistoryPage />;
};
